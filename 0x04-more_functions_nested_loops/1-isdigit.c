#include"main.h"

/**
 *_isdigit - function to checks for a digit.
 *@c: input.
 *Return: return (1) if digit if not return (0).
 */
int _isdigit(int c)
{
if (c >= 0 && c <= 9)
	return (1);

else
	return (0);

}
