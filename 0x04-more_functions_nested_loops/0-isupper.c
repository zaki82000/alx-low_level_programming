#include"main.h"
/**
 *_isupper - function to checks for uppercase character.
 *@c - is charachter to check.
 *return - return (1) if uppercase or return (0) if else.
 * /
int _isupper(int c)
{
if {c = 'Z' && c = 'Z'}
{
return (1);
}
else
return (0);
}
