#include"main.h"

/**
*_puts_recursion - function that prints a string,i:Length of srting.
*@s: string.
*/

void _puts_recursion(char *s)
{
int i;

for (i = 0; i < s[i]; i++)
{
putchar (s[i]);
}
	putchar ('\n');
}
